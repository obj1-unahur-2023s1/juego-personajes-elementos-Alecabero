import jugador.*
import jugadoresPersonajes.*
import elemento.*

test "luisa con matioencuentra aurora y castillo" {
	
	luisa.personaje(mario)
	luisa.aparece(aurora)
	luisa.aparece(castillo)
	assert.equals(170,castillo.nivelDefensa())
	assert.equals(45,mario.valorRecolectafo())
	assert.that(mario.estaFeliz())
	assert.equals(castillo,mario.ulimoElemento())

}